Q1:
	1) 	int (*pMyFunc) (int);

	2) 	Declare a function pointer array that holds 5 function pointers with the name "ptr".
		and "ptr" is of type (int*).

	3) 	void (*f) ();
		f = (void(*) () ) & f1;
	    f = (void(*) () ) & f2;

Q2:

	1) output: 40

